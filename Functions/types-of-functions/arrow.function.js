//! ======================================= Arrow Function =======================================

// Introduced in ECMAScript 6 (ES6) for concise syntax.
// Lexical scoping of the this keyword.

const myArrowFunction = (x, y) => {
  return x + y;
};
