//! ====================================== Anonymous Function ======================================

// Functions without a name.
// Often used as callbacks.

const result = function (x, y) {
  return x + y;
};
