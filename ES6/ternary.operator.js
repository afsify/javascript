//! ======================================== Ternary  Operator ========================================

// The ternary operator provides a concise way to write a simple if-else statement in a single line.

let isEven = y % 2 === 0 ? "Even" : "Odd";
console.log(isEven);
